// @duel/relay-server
//
// Optional thin relay for friends-only online play. Forwards Commands between
// clients on the protocol defined in @duel/shared. The authoritative engine
// still runs on each client — the relay never runs game logic.

export {};
