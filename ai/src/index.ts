// @duel/ai
//
// Opponent AI(s). Implements the AI interface defined in @duel/shared:
//   (redactedState) => Command
// The AI never sees hidden information — only what the redactor allows.

export {};
