// @duel/engine
//
// Pure rules engine. No I/O, no Date.now(), no Math.random() outside an
// injected seeded RNG. Signature lands as (State, Command) -> State.
// Depends only on @duel/shared.

export {};
