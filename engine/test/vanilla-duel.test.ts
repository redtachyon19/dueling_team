// Milestone marker for the first real engine pass: a complete duel using only
// vanilla (no-effect) monsters, set/summon, attacks, and life points.
// Once this stops being a placeholder, "vanilla duel" is the bar.

import { describe, it, expect } from "vitest";

describe("vanilla duel (placeholder)", () => {
  it("is reserved for the first end-to-end engine test", () => {
    expect(true).toBe(true);
  });
});
